---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflared-tunnel-token
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: cloudflared-tunnel-token
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: homelab-cloudflared-tunnel-token
        property: credential
